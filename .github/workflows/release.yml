name: Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: Syzygy-Hub/.github/.github/workflows/ios-release.yml@main
    secrets: inherit
