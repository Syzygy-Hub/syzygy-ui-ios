import SwiftUI

#Preview("BackButton - Light") {
    BackButton {}
        .padding()
        .preferredColorScheme(.light)
}

#Preview("BackButton - Dark") {
    BackButton {}
        .padding()
        .preferredColorScheme(.dark)
}
